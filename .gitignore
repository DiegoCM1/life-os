# Claude Code — local-only MCP & config (not shared)
.mcp.json
.claude/settings.local.json

# Supabase agent skills (installed locally per-developer)
.agents/
.claude/skills/
skills-lock.json

# Dependencies & builds
node_modules/
.next/
.venv/
__pycache__/

# Local env files (secrets live in Vercel/Railway env vars)
.env
.env.local
